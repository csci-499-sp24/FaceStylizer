import { MenuProps } from '@nextui-org/menu';
import { Ref, ReactElement } from 'react';

interface Props<T> extends Omit<MenuProps<T>, "menuProps"> {
}
type DropdownMenuProps<T = object> = Props<T> & {
    ref?: Ref<HTMLElement>;
};
declare const _default: <T = object>(props: DropdownMenuProps<T>) => ReactElement;

export { DropdownMenuProps, _default as default };
